/**
 * Generated TypeScript types from the FastAPI OpenAPI schema.
 *
 * Regenerate with `bun run typegen:openapi` (from the repo root) after running
 * the API at least once so `apps/api/openapi.json` exists.
 */

export type {} from './generated/schema';
