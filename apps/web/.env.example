VITE_API_BASE_URL=http://localhost:8000
# Neon Auth: use same-origin proxy path in production (/neon-auth).
# For local Safari testing, either run `wrangler pages dev` or set the full
# Neon Auth URL (Chrome only — Safari blocks third-party cookies).
# VITE_NEON_AUTH_URL=/neon-auth
# VITE_NEON_AUTH_URL=https://ep-xxx.neonauth....aws.neon.tech/neondb/auth
